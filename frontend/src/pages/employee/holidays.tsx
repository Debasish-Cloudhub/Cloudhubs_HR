import{useEffect,useState} from 'react';import Layout from '../../components/common/Layout';import{getHolidays} from '../../services/api';import Head from 'next/head';
export default function EmployeeHolidays(){
  const[holidays,setHolidays]=useState<any[]>([]);
  useEffect(()=>{getHolidays(new Date().getFullYear()).then(r=>setHolidays(r.data)).catch(()=>{});},[]);
  return <><Head><title>Holidays - CloudHub HR</title></Head><Layout><h1 className="font-bold text-2xl text-slate-800 mb-6">Holiday Calendar {new Date().getFullYear()}</h1><div className="card p-0 overflow-hidden"><table className="w-full"><thead className="bg-slate-50 border-b"><tr><th className="table-header">Holiday</th><th className="table-header">Date</th><th className="table-header">Day</th><th className="table-header">Type</th></tr></thead><tbody className="divide-y divide-slate-100">{holidays.map(h=><tr key={h.id} className="hover:bg-slate-50"><td className="table-cell font-medium">{h.name}</td><td className="table-cell">{new Date(h.date).toLocaleDateString('en-IN',{day:'numeric',month:'long',year:'numeric'})}</td><td className="table-cell">{new Date(h.date).toLocaleDateString('en-IN',{weekday:'long'})}</td><td className="table-cell"><span className={h.is_optional?'badge-pending':'badge-approved'}>{h.is_optional?'Optional':'Mandatory'}</span></td></tr>)}{holidays.length===0&&<tr><td colSpan={4} className="table-cell text-center text-slate-400 py-8">No holidays</td></tr>}</tbody></table></div></Layout></>;
}