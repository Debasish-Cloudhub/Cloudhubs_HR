import{useEffect,useState} from 'react';import Layout from '../../components/common/Layout';import{getEmployees,setSalaryComponents,generatePayslip,getSalaryRecords,downloadPayslip} from '../../services/api';import toast from 'react-hot-toast';import{Download} from 'lucide-react';import Head from 'next/head';
export default function AdminPayroll(){
  const[employees,setEmployees]=useState<any[]>([]);const[records,setRecords]=useState<any[]>([]);const[selEmp,setSelEmp]=useState('');const[comp,setComp]=useState({basic:'',hra:'',allowances:'',bonus:'',pf_deduction:'',professional_tax:'',income_tax:''});const[genForm,setGenForm]=useState({employee_id:'',month:new Date().getMonth()+1,year:new Date().getFullYear()});
  const loadRecords=()=>getSalaryRecords().then(r=>setRecords(r.data)).catch(()=>{});
  useEffect(()=>{getEmployees().then(r=>setEmployees(r.data)).catch(()=>{});loadRecords();},[]);
  const saveComp=async()=>{if(!selEmp)return toast.error('Select employee');try{await setSalaryComponents({...Object.fromEntries(Object.entries(comp).map(([k,v])=>[k,parseFloat(v as string)||0])),employee_id:parseInt(selEmp)});toast.success('Saved');}catch{toast.error('Failed');}};
  const generate=async()=>{if(!genForm.employee_id)return toast.error('Select employee');try{await generatePayslip({...genForm,employee_id:parseInt(genForm.employee_id)});toast.success('Generated');loadRecords();}catch{toast.error('Failed');}};
  const download=async(id:number)=>{try{const r=await downloadPayslip(id);const url=URL.createObjectURL(new Blob([r.data],{type:'application/pdf'}));const a=document.createElement('a');a.href=url;a.download='payslip.pdf';a.click();}catch{toast.error('Failed');}};
  return <><Head><title>Payroll - CloudHub HR</title></Head><Layout><h1 className="font-bold text-2xl text-slate-800 mb-6">Payroll</h1><div className="grid grid-cols-1 lg:grid-cols-2 gap-6 mb-6"><div className="card"><h2 className="font-semibold text-slate-700 mb-4">Set Salary Components</h2><div className="space-y-3"><select value={selEmp} onChange={e=>setSelEmp(e.target.value)} className="input"><option value="">Select Employee</option>{employees.map(e=><option key={e.id} value={e.id}>{e.first_name} {e.last_name} ({e.employee_id})</option>)}</select>{['basic','hra','allowances','bonus','pf_deduction','professional_tax','income_tax'].map(k=><div key={k}><label className="label capitalize">{k.replace('_',' ')}</label><input type="number" value={(comp as any)[k]} onChange={e=>setComp(p=>({...p,[k]:e.target.value}))} className="input" placeholder="0"/></div>)}<button onClick={saveComp} className="btn-primary">Save</button></div></div><div className="card"><h2 className="font-semibold text-slate-700 mb-4">Generate Payslip</h2><div className="space-y-3"><select value={genForm.employee_id} onChange={e=>setGenForm(p=>({...p,employee_id:e.target.value}))} className="input"><option value="">Select Employee</option>{employees.map(e=><option key={e.id} value={e.id}>{e.first_name} {e.last_name}</option>)}</select><div className="grid grid-cols-2 gap-3"><div><label className="label">Month</label><select value={genForm.month} onChange={e=>setGenForm(p=>({...p,month:parseInt(e.target.value)}))} className="input">{Array.from({length:12},(_,i)=><option key={i+1} value={i+1}>{new Date(2024,i).toLocaleString('en',{month:'long'})}</option>)}</select></div><div><label className="label">Year</label><input type="number" value={genForm.year} onChange={e=>setGenForm(p=>({...p,year:parseInt(e.target.value)}))} className="input"/></div></div><button onClick={generate} className="btn-primary">Generate</button></div></div></div><div className="card p-0 overflow-hidden"><table className="w-full"><thead className="bg-slate-50 border-b"><tr><th className="table-header">Employee</th><th className="table-header">Period</th><th className="table-header">Gross</th><th className="table-header">Net</th><th className="table-header">PDF</th></tr></thead><tbody className="divide-y divide-slate-100">{records.map(r=><tr key={r.id} className="hover:bg-slate-50"><td className="table-cell">{r.employee_id}</td><td className="table-cell">{new Date(r.year,r.month-1).toLocaleString('en',{month:'short',year:'numeric'})}</td><td className="table-cell">₹{r.gross_salary?.toLocaleString('en-IN')}</td><td className="table-cell font-semibold text-green-600">₹{r.net_salary?.toLocaleString('en-IN')}</td><td className="table-cell"><button onClick={()=>download(r.id)} className="flex items-center gap-1 text-xs text-blue-600 hover:text-blue-800"><Download size={14}/>Download</button></td></tr>)}{records.length===0&&<tr><td colSpan={5} className="table-cell text-center text-slate-400 py-8">No payslips</td></tr>}</tbody></table></div></Layout></>;
}